video min: 160x50
play
pause


ctrl+F ctrl+R

WysiwygApp.kendoEditor.exec("backColor", {value: "yellow"})

a = $("#design-panel")
b = $("#span")
a.scrollTop(b.offset().top - a.offset().top + a.scrollTop());

b = WysiwygApp.kendoEditor.getRange().startContainer.parentNode

查找之后高亮效果采用修改html的方式实现，但做copy或save操作时，要先把这个高亮代码去掉，然后做copy或save，然后再加回去。

default页面没有尺寸:
    default-editor的widht, height, 改用min-width, min-height
其它solution则有固定尺寸:
    跟当前一样，用widht, height
    